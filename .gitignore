# OS
.DS_Store
Thumbs.db

# Never commit secrets or local runtime state, even if a tool writes them at the repo root.
.env
.env.*
.wrangler/
.edgespark/
node_modules/

# Each template also manages its own ignores under its subdirectory.
